---
description: Create project plan using project-planner agent. No code writing - only plan file generation.
argument-hint: [detalhes da tarefa]
---

Execute o workflow do AG Kit "/plan".

Leia o arquivo `.agents/workflows/plan.md` por completo e siga os passos exatamente como descritos. Ele pode referenciar agentes especialistas em `.agents/agent/` e skills em `.agents/skills/<skill>/SKILL.md` — carregue esses arquivos conforme necessário.

Argumentos do usuário: $ARGUMENTS
