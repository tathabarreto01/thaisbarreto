---
description: Test generation and test running command. Creates and executes tests for code.
argument-hint: [detalhes da tarefa]
---

Execute o workflow do AG Kit "/test".

Leia o arquivo `.agents/workflows/test.md` por completo e siga os passos exatamente como descritos. Ele pode referenciar agentes especialistas em `.agents/agent/` e skills em `.agents/skills/<skill>/SKILL.md` — carregue esses arquivos conforme necessário.

Argumentos do usuário: $ARGUMENTS
